#this program will calculate the cost per square inch of a pizza
#Written by David Pick
from math import *

size = input("Enter the diameter of the pizza: ")
price = input("Enter the price of the pizza: ")

#calculate size in square inchs of pizza
size = (size / 2)^2
area = pi * size
#calculate cost
cost = area / price

print "The price per square inch is ", cost
