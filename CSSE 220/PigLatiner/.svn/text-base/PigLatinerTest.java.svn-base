import junit.framework.TestCase;


public class PigLatinerTest extends TestCase {

	public void testTransform() {
		//fail("Not yet implemented");
		assertEquals("uttonbay", PigLatiner.transform("button"));
		assertEquals("oughday", PigLatiner.transform("dough"));
		assertEquals("away", PigLatiner.transform("a"));
		assertEquals("tay", PigLatiner.transform("t"));
		assertEquals("eagleway", PigLatiner.transform("eagle"));
		//assertEquals(1, 1);
	}

}
