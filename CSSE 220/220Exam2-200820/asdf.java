
public class asdf<T> {
	public min (T[] array) {
		private <T> current;
		current = array[0];
		for (int = 0; i < array.length - 1; i++) {
			if (array[i+1].compareTo(array[i]) < 0)
				current = array[i+1];
		}
	}
}
